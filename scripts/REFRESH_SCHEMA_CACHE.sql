-- ============================================================
-- 刷新 Supabase Schema Cache
-- ============================================================
-- 用途：通知 PostgREST 重新載入資料庫 schema
-- 執行後需等待幾秒鐘讓 cache 更新
-- ============================================================

NOTIFY pgrst, 'reload schema';

SELECT
  '✅ Schema cache 刷新請求已發送' as status,
  '請等待 5-10 秒後重新測試' as next_step;
