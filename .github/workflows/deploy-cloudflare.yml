name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - main
      - 'feature/**'
      - 'fix/**'
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Next.js with OpenNext
        env:
          # Build-time 環境變數 (NEXT_PUBLIC_* 會在 build 時嵌入 JS)
          NEXT_PUBLIC_SUPABASE_URL: https://oubsycwrxzkuviakzahi.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im91YnN5Y3dyeHprdXZpYWt6YWhpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM5MDM5MDUsImV4cCI6MjA3OTQ3OTkwNX0.Ltz-HSAlcWHV6nsEkckD88ERbCVZSE9C8vNhbW7ELGA
          NEXT_PUBLIC_APP_URL: https://quote24.cc
        run: pnpm run build

      # 生產環境部署 (main 分支)
      - name: Deploy to Production
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: f9916b95d011e8ad2a3fe10883053b0f
        run: |
          npx wrangler deploy
          echo "## 🚀 Production Deployment" >> $GITHUB_STEP_SUMMARY
          echo "**URL**: https://quote24.cc" >> $GITHUB_STEP_SUMMARY

      # 預覽環境部署 (非 main 分支)
      - name: Deploy to Preview
        if: github.ref != 'refs/heads/main' && github.event_name == 'push'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: f9916b95d011e8ad2a3fe10883053b0f
        run: |
          # 使用 preview 環境 (繼承 KV, R2 等綁定)
          npx wrangler deploy --env preview
          echo "## 🔍 Preview Deployment" >> $GITHUB_STEP_SUMMARY
          echo "**Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**URL**: https://quotation-system-preview.acejou27.workers.dev" >> $GITHUB_STEP_SUMMARY

      # PR 只做 build 測試，不部署
      - name: Build Test Summary (PR)
        if: github.event_name == 'pull_request'
        run: |
          echo "## ✅ Build Test Passed" >> $GITHUB_STEP_SUMMARY
          echo "PR build completed successfully. Merge to deploy." >> $GITHUB_STEP_SUMMARY
