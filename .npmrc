# 使用 pnpm 作為套件管理器
packageManager=pnpm

# 自動安裝對等依賴
auto-install-peers=true

# 套件引擎檢查
engine-strict=true

# 解決 Cloudflare Workers 部署相關設定
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=*supabase*
shamefully-hoist=false

# 提高安裝速度
prefer-frozen-lockfile=false